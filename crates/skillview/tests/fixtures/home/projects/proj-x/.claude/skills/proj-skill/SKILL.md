---
name: proj-skill
description: Project-local Claude skill (its `.claude` ancestor is not directly under the fixture home), so skillview should classify it as `claude-project` rather than `claude-global`.
---

# proj-skill

A skill living inside a project-local `.claude/` directory used to verify
the `claude-project` vs `claude-global` distinction.
