---
name: orphan-skill
description: Skill that lives under a generic `skills/` directory with no recognized agent ancestor, exercising the unknown-agent fallback classification path.
---

# orphan-skill

A skill outside every recognized agent root; classifier should fall back to
`unknown` agent and `unknown` root kind.
