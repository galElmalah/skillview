---
name: codex-helper
description: Codex-specific helper skill used to exercise the codex agent classification path. Distinctive name so usage scanning treats it as high-confidence.
---

# codex-helper

A skill that lives only under the codex root so we can verify that the
agent classifier returns `codex` for files under `~/.codex/skills/`.
